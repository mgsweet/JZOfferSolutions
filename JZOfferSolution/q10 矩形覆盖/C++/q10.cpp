class Solution {
public:
    int rectCover(int n) {
        vector<int> arr({0, 1, 2});
        if (n < 0) return 0;
        else if (n <= 2) return arr[n];
        else {
            float result =
            return arr[n];
        }
    }
};
